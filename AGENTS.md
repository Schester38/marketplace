# AGENTS.md — Mboppi Marketplace

Contexte de travail pour toute session IA sur ce dépôt. Lire ce fichier avant de modifier quoi que ce soit.

## Vue d'ensemble

Marketplace **Mboppi** (Cameroun et Afrique) : vente en ligne, boutiques physiques, vendeurs indépendants, créateurs, livreurs, commandes par téléphone/WhatsApp et paiement mobile.

- **Client** : React 18 + Vite 5 (`client/`, dev 5173, proxy `/api` → `localhost:4000`), version `1.57.64`.
- **Serveur** : Express 4 (`server/`, port 4000), PostgreSQL via Supabase (`server/db.js` : `DATABASE_URL_POOLED`, fallback `DATABASE_URL`, `ssl rejectUnauthorized: false` ; exports `q()`, `withTransaction()`, `initDb()`).
- **Finance** : `server/services/payouts.js` — `computeRedistribution`, `normalizeWalletPrimary`, seuils de commission. Il n'existe **pas** de `server/finance.js`.
- **Déploiement** : Vercel, entrée serverless `api/index.js` → `server/app.js` + `initDb()`. `vercel.json` route `/api/*`, `/produit/:id`, `/boutique/:id`, `/createur/:id`, `/ville/:x`, `/offre/:id`, `/sitemap.xml`, `/` vers l'API, le reste vers le SPA.
- **Prod** : `https://mboppi-mboppi.vercel.app` (client + API). `BASE_URL`, `SITE_URL`, `PUBLIC_URL` par défaut sur ce domaine.
- **Domaine personnalisé** : `mboppi.qd.je` (gratuit, namespace DigitalPlat) enregistré le 17/09/2026 — mais **inutilisable sur Vercel** : l'ajout au projet renvoie `Error: Not authorized to use mboppi.qd.je (403)` et l'apex parent `qd.je` répond `Error: The domain qd.je already exists under a different context` ⇒ la zone `qd.je` est **déjà rattachée à un autre compte Vercel** (même cas pour `*.dpdns.org` de DigitalPlat), donc Vercel refuse tous ses sous-domaines. Contrôles effectués en CLI le 17/09/2026 (puis annulés) : `mboppitest.duckdns.org` ✅ accepté, `mboppi-test2.eu.org` ✅ accepté, `mboppitest.example.com` ❌ 403, `mboppi-check-tmp.dpdns.org` ❌ 403. ⇒ Règle observée : **un sous-domaine n'est accepté que si sa zone parente n'est pas déjà revendiquée par un autre compte Vercel** (`eu.org` et `duckdns.org` OK, DigitalPlat ❌). Solutions : `mboppi.eu.org` (gratuit, validation manuelle de plusieurs jours chez NIC.EU.ORG + DNS gratuit chez deSEC/Hurricane Electric) **ou** un vrai domaine (candidats **libres** vérifiés par RDAP le 17/09/2026 : `mboppishop.com`, `mboppimarket.com`, `mboppiafrica.com`, `mboppi.shop`, `mboppi.store`, `mboppi.online`, `mboppimarket.xyz`). ⚠️ `mboppi.com` **n'appartient pas** au projet (parqué, en vente ~1 895 $ via Afternic/HugeDomains) : retiré du compte Vercel le 17/09/2026 (le compte n'a alors **aucun domaine** propre). La bascule du code vers un domaine personnalisé est **déjà préparée** et pilotée par l'environnement (`VITE_SITE_URL`, `SITE_URL`, `PUBLIC_URL`) — voir « Bascule vers un domaine personnalisé ».

## Rôles utilisateurs

`users.role` accepte `('shop','seller','client','creator','livreur')` ; la contrainte est étendue à `'admin'` par une migration (`db.js`). L'admin est un utilisateur virtuel (id 0), authentifié par `ADMIN_PASSWORD` → `POST /api/admin/pass`.

> **Compte administrateur personnel** : l'admin virtuel ne peut pas recevoir les notifications (push/cloche) car `push_subscriptions` et `notifications` référencent `users.id`. `POST /api/admin/account/register` (token admin requis) crée le vrai compte de l'admin (rôle `admin`, email déjà vérifié, adhésion active) et renvoie le JWT → connexion immédiate. Les formulaires connexion/inscription correspondants sont dans le panneau Admin (`Admin.jsx`, carte « 👤 Compte administrateur »), visibles uniquement après le mot de passe admin.
>
> **Notifications admin** : `server/services/adminNotify.js` (`notifyAdmins`) diffuse push + cloche 🔔 à tous les comptes de rôle `admin` (ids en cache 60 s, jamais bloquant, erreurs avalées). Événements couverts : nouvelle inscription (email + Google, `auth.js`), vente (`sales.js` POST `/`, `purchases.js`, `orders.js`), livraison confirmée (`sales.js` `/:id/deliver`), adhésion payée (manuelle `admin.js` `referrals/:id/pay` + `users/:id/admin-approved`, en ligne `ikeepay.js` `activateMembershipUser`), don déclaré (`donations.js`) ou payé en ligne (`ikeepay.js` `completeDonation`), demande de retrait d'activation (`activationWithdrawals.js`, en plus de WhatsApp + email).

- **shop** : boutique (produits illimités), promos éclair, partage, paiement des commissions.
- **seller** : vendeur indépendant, code vendeur 6 caractères, commissions de vente et de parrainage.
- **creator** : publie **uniquement des produits digitaux** (fichiers téléchargeables, catégorie par défaut « Digital ») et gère ses créations/offres Verone. Moyens de paiement partagés avec les boutiques (`shop_payment_methods`).
- **client / livreur** : espaces distincts ; le livreur confirme les livraisons (`/:id/deliver`).
- **Adhésion : vendeur, boutique ET créateur paient** — blocage global activé en production (`platform_settings` clé `membership_gate` = `"all"` ; défaut du code : `"seller"`, bascule dans Admin → Système de paiement, voir `server/services/membershipGate.js`). Frais par rôle (`MEMBERSHIP_FEES`, `server/fees.js`, re-exportés par `server/auth.js`) : **vendeur 1 500 XAF, boutique 2 500 XAF, créateur 2 500 XAF** / 30 jours (`MEMBERSHIP_DAYS`). `roleRequired` renvoie **402 MEMBERSHIP_REQUIRED** pour tout rôle soumis tant que `admin_approved` est faux et `membership_expires_at` n'est pas dans le futur. Clients et livreurs accèdent **directement** à leur espace (aucun frais) — l'admin vérifie les comptes dans le panneau.
- Auth : JWT (`JWT_SECRET` **requis, min 32 caractères** sinon le serveur refuse de démarrer), 24 h. Email verification (24 h TTL, `SITE_URL`), Google OAuth optionnel.

## Commission et argent

### Frais de service — 0 %, deux systèmes de paiement (manuel ↔ automatique)

L'admin bascule entre les deux systèmes via le panneau **Administration → Système de paiement** (bouton « Basculer vers le manuel / automatique »). Le mode est stocké dans `platform_settings` (clés `payment_mode`, `ikeepay_public_key`, `ikeepay_secret_key`) et le **serveur fait autorité** : les routes de payin sont refusées en mode manuel.

- **Mode manuel (défaut)** : adhésions et dons payés hors plateforme (Mobile Money direct, virement UBA, MoneyFusion) ; l'admin valide chaque adhésion (`PATCH /api/admin/users/:id/admin-approved`, `POST /api/admin/referrals/:id/pay`) et la page d'adhésion affiche les instructions de virement.
- **Mode automatique (iKeePay — PAYIN uniquement, pas de payout)** : l'adhésion (**vendeur, boutique ou créateur** — `POST /api/payments/membership-payin`, refusé pour un rôle non soumis au blocage ou en mode manuel) et le don (`POST /api/payments/donation-payin`) sont payés en ligne via le checkout inline `https://ikeepay.com/checkout/v1/inline` (iframe + `postMessage`). Un **webhook** `POST /api/ikeepay/webhook?k=SECRET` (deux formats gérés : `payment.success` et `transaction.updated/created` avec `data.status`/`data.type` ; référence cherchée dans la racine, `data` **et** `data.data`) confirme la transaction → activation immédiate du compte (`admin_approved = TRUE`, 30 jours, dans la **même transaction** que le passage à `completed`) et notification du parrain si adhésion parrainée. **Les versements vendeur/parrain restent manuels** dans les deux modes.
- **Sécurité webhook (TOKEN OBLIGATOIRE)** : le webhook est protégé par un secret auto-généré (`platform_settings` clé `ikeepay_webhook_secret`, 32 hex) accepté via `?k=...` ou le header `x-ikeepay-token`, comparé en temps constant (`timingSafeEqual`). **Sans token valide → 403 `invalid_webhook_token`.** L'URL complète (avec token) est affichée avec bouton « Copier » dans le panneau Admin → Système de paiement : c'est **elle** qui doit être enregistrée chez iKeePay. Montant + devise vérifiés au webhook (XAF/XOF tolérés, parité CFA) ; clé secrète iKeePay jamais exposée au client ; origine iKeePay autorisée (`originCheck` + CSP `frame-src`/`form-action` dans `vercel.json` **et** `server/security.js`).
- **Fiabilité / auto-réparation (post-1.52.2, non versionné)** :
  - chaque webhook reçu est **journalisé AVANT traitement** dans `payment_webhook_logs` (un crash/timeout laisse une trace exploitable) ;
  - **chaque webhook rejeté pour token invalide est journalisé** (`event='auth_rejected'`, `handled=TRUE` — jamais utilisé par la réconciliation) : un paiement « en attente » sans trace est désormais visible dans le panneau Admin → Paiements en ligne (bloc « santé du webhook » : reçus 24 h / rejetés 24 h / dernier événement, + bandeau rouge d'alerte « URL iKeePay obsolète ») ;
  - `getWebhookSecret` est **lecture stricte** : une erreur de base de données (bascule direct ↔ pooler Supabase, mot de passe pooler invalide, EMAXCONN) **ne régénère JAMAIS le secret** (sinon l'URL enregistrée chez iKeePay devient invalide → 403 permanents) ; secret indisponible → **503 `webhook_secret_unavailable`** (iKeePay réessaie) au lieu d'un 403 silencieux ou d'une auth acceptée à secret vide ;
  - la purge des paiements en attente > 30 min **archive** (`status='expired'`) au lieu de supprimer : la liste admin filtre les `expired`, mais ils restent réconciliables **24 h** ;
  - réconciliation webhook tardive par **email prioritaire puis montant** (lignes `pending` **et** `expired`, ≤ 24 h) ; balayage des logs non rattachés élargi à **30** lignes (les webhooks auth_rejected/unsupported ne masquent plus un vrai payment.success) ;
  - `GET /api/payments/membership-status` (auth requis) : filet d'activation — si un paiement du compte est `completed` (< 24 h) alors que le compte n'est pas actif, il est activé à la volée (idempotent) ; sinon réconciliation depuis les logs de webhooks non rattachés (cooldown 5 s, budget 4 s) ;
  - la page d'adhésion **sonde** ce endpoint toutes les 4 s (6 min max) avec diagnostic dans la console (`[adhésion] statut : ...`, liste des 5 derniers webhooks) → redirection automatique dès activation (via l'effet `membershipActive` d'`App.jsx`) ;
  - secours admin : boutons « Marquer complété » sur dons **et** adhésions (`POST /api/admin/payments/{donations,memberships}/:id/complete`).

Les moyens de paiement sont enregistrés par chaque espace pour permettre les transferts directs entre parties.

**Panneau admin (quel que soit le mode)** : la section **Utilisateurs** (recherche, ouvrir/fermer, vérifier, adhésion) et la section **Retraits d'activation** sont **toujours visibles** pour que l'admin puisse vérifier les comptes et payer les retraits (qui restent manuels dans les deux modes). Seule la section **Parrainages** (marquage « payé » manuel) est propre au mode manuel ; en mode automatique, elle est remplacée par le suivi des paiements en ligne iKeePay.

**Organisation par onglets** (`Admin.jsx`, constante `ADMIN_TABS`) : le panneau est découpé en 9 onglets — 📊 **Vue d'ensemble** (compte admin, statistiques, rôles, visites), 🛰️ **Livraisons** (suivi GPS ; la modale carte reste globale), 📣 **Communication** (campagne, campagnes programmées, messages, newsletter), 💰 **Paiements** (système de paiement + paiements en ligne iKeePay), 👥 **Utilisateurs** (comptes + parrainages en mode manuel), 💸 **Retraits d'activation**, 🛍️ **Produits**, 📚 **Générateur** (création d'ebooks/PDF, module isolé `server/routes/generator.js` + tables `gen_*`), ⚙️ **Système** (robot WhatsApp, maintenance des images). Une seule famille de sections est rendue à la fois (`{adminTab === "x" && (<>…</>)}`, barre d'onglets `.admin-tabs` / `.admin-tab` dans `client/src/styles.css`), l'onglet actif étant mémorisé dans `localStorage` (`admin_tab`). Aucun état, `useEffect` ni appel API n'est conditionné par l'onglet : toutes les données continuent d'être chargées (au montage + toutes les 30 s), seul l'affichage change. Pastilles rouges sur **Paiements** (paiements en ligne en attente, mode automatique) et **Retraits d'activation** (demandes en attente) ; messages globaux (erreur/chargement) et modale GPS restent hors onglets.

### Répartition d'une vente — `server/services/payouts.js` `computeRedistribution`

`totalPrice = prix × quantité` (le prix de référence : catalogue OU promo éclair active — un prix client arbitraire ne modifie jamais le calcul). Champs d'une vente : `commission`, `referral_commission` (2 % parrainage), `delivery_fee` (saisie par le livreur, défaut 0).

- `shopAmount = totalPrice − commission − referralCommission` (ce que touche la boutique)
- `sellerAmount = commission` (le vendeur)
- `referrerAmount = referralCommission` (le parrain)
- `livreurAmount = deliveryFee` (le livreur)

Le paiement des gains est **manuel** et comptabilisé dans `wallet_transactions` **sans frais** :
- vendeur : `POST /api/sales/:id/pay` (boutique, **preuve `data:` obligatoire**) → `commission_credit` ;
- parrain : `POST /api/sales/:id/pay-referral` → `referral_credit` ;
- groupé (boutique) : `POST /api/sales/grouped-pay` (`kind: seller|referral`, `seller_id`, `proof`).

### Seuils de commission — `payouts.js`

`REFERRAL_CLAIM_THRESHOLD = COMMISSION_CLAIM_THRESHOLD = 5000` XAF.

- **Vente unitaire** : `POST /api/sales/:id/pay` et `POST /api/sales/:id/claim` (vendeur) sont refusés si la **commission de cette vente** < 5 000 XAF ; `POST /api/sales/:id/claim-referral` pareil sur le parrainage de la vente. `POST /api/sales/:id/pay-referral` (simple, parrain) n'a **pas** de seuil.
- **Groupé** : le vendeur réclame (`POST /api/sales/grouped-claim`, `kind: sale|referral`, `shop_id`) puis la boutique paie (`grouped-pay`) ; le **cumul** par bénéficiaire et par boutique doit atteindre 5 000 XAF.

### Parrainage client — 2 % (clients affiliés)

1. Un **client** s'inscrit avec le `seller_code` d'un vendeur (`ref`) → son compte est marqué `referred_by = id_du_vendeur`, rôle forcé `client`.
2. Quand ce client **authentifié** (`req.user`) achète, `referral_commission = prix × quantité × 2 %` et `referred_by` est enregistré sur la vente (`purchases.js`, `orders.js`). Un achat invité ne déclenche **jamais** le 2 %.
3. Le 2 % est **réclamé puis payé manuellement** par la boutique (`claim-referral` / `pay-referral` / groupé) ; aucun versement automatique.

### Parrainage d'activation vendeur (distinct)

Un nouveau **vendeur** s'inscrit avec le code d'un autre vendeur (`ref_seller`) → `referred_by`. Le rôle du parrainé est **forcé à `seller`** à l'inscription (email ou Google), même si le lien comportait un rôle `creator` — la doc « vendeur/créateur » est obsolète. Quand l'admin marque l'adhésion payée (`POST /api/admin/referrals/:id/pay` → `membership_paid_at`), le parrainé est simultanément **activé** (`admin_approved = TRUE`, `membership_expires_at = now() + 30 jours`) et le parrain gagne **1 000 XAF par parrainé**. 

La notification push au parrain (type `activation_referral_paid`) est déclenchée **à chaque nouvelle adhésion payée** (balance en hausse), que l'admin passe par l'onglet **Parrainages** (`POST /api/admin/referrals/:id/pay`) ou par l'approbation dans l'onglet **Utilisateurs** (`PATCH /api/admin/users/:id/admin-approved`, qui pose aussi `membership_paid_at`). `membership_paid_at` n'est posé **que lors d'une approbation** (`admin_approved = TRUE`) : désapprouver ne marque jamais l'adhésion payée. Un re-clic sur un parrainé déjà payé n'envoie pas de doublon de notification.

Il retire le cumul via `activation-withdrawals` (montant **multiple de 1 000**, **minimum 5 000 XAF**) ; l'admin paie la demande (`POST /api/admin/activation-withdrawals/:id/pay`). Aucun reversement automatique.
**Comptabilité** : une demande payée insère un **débit** `payout_debit` dans `wallet_transactions` sans crédit d'activation préalable — le solde du wallet (`GET /api/wallet/me`) peut donc être **négatif**. La balance vit dans `activation_withdrawals`, séparée du wallet.

### Commission de vente d'un produit

- `products.commission_percent` est choisi par la **boutique à la création du produit** (0-100). Commission vente seller = `prix × commission_percent % × quantité`.
- Pendant une **promo éclair**, la ligne `flash_promotions.commission_percent = 0` (le produit est masqué des catalogues ; le vendeur est exclu).
- `wallet_transactions.transaction_type` inclut `commission_credit`, `referral_credit`, `payout_debit`, `adjustment`, `online_collect`, `online_payout` (les deux derniers sont des résidus iKeePay, jamais émis).

### Wallet et moyens de paiement

- **Wallet** (`server/routes/wallet.js`) : `GET /api/wallet/me` réservé à **seller, creator, livreur** ; `wallet_transactions`, `balance = SUM(amount)` (montants signés), devise XAF.
- Moyens de paiement : `shop_payment_methods` / `seller_payment_methods` / `livreur_payment_methods` (PK = `*_id`, `full_name`, `wallets` jsonb — liste `{name, value, primary}`, upsert `ON CONFLICT`).
- `normalizeWalletPrimary` (payouts.js) garantit **au plus un** wallet `primary` (le premier par défaut). Il n'existe **pas** de priorité « orange » ni de préfixe pays automatique.
- Pays et moyens de paiement : liste statique dans `client/src/config.js`. Les coordonnées servent aux transferts directs ; Mboppi ne collecte pas les paiements.

## Promotions éclair (flash promotions)

- **Règles serveur** (`server/routes/flashPromotions.js`) : **uniquement les boutiques**, **max 1 promo sur les 7 derniers jours** (MAX_WEEKLY_PER_SHOP = 1, compté sur starts_at, MIN_WEEK_GAP_DAYS = 7), durée max **24 h** (MAX_MINUTES = 1440, défaut 180), prix promo < prix produit, **commission 0** (colonne commission_percent de la ligne), purgées à expiration.
- `GET /api/flash-promotions` : liste publique `ends_at > now() AND quantity > 0`, `ORDER BY ends_at ASC LIMIT 20`.
- **Produit masqué pendant la promo** : `NOT EXISTS (SELECT 1 FROM flash_promotions fp WHERE fp.product_id = p.id AND fp.ends_at > now())` partout (catalogues, boutique, créateur, SEO, compteurs). Seule `/produit/:id` reste accessible (landing promo, prix via `flash_promo`).
- **Prix promo côté client** : `product.flash_promo` (`{id, price, discount_percent, commission_percent, commission, starts_at, ends_at, duration_minutes}`) dans ProductDetail.jsx, PurchasePage.jsx, store.jsx (panier, old_price), ProductCard. `data-flash` dans les commandes.
- **Popup publique** (FlashPromoPopup.jsx) : rendue globalement dans App.jsx (toutes les pages). Promos actives superposées (`.flash-popup-stack`, `position: fixed`, bas à droite), rotation **5 s** (ROTATE_MS = 5000), max 4, refresh 30 s. Titre centré « ⚡ PROMOTION DU JOUR ». La croix ✕ ferme tout + localStorage `mboppi_flash_dismissed`.
- **CSS important** : `.flash-popup` en `position: absolute; bottom: 0` — **ne jamais les ancrer en top** (bug corrigé). Cartes inactives : opacité 0.55, translateY(-8px), pointer-events: none.
- Form boutique : `ShopDashboard.jsx` (`flash-promo-form`), carte `FlashPromoCard` (partage 🔗 / annulation). URL directe : `/produit/{id}` = landing promo (badge, compte à rebours, ancien prix barré).
- **État actuel (code)** : `POST /api/sales` ne vérifie **pas** `flash_promotions` — pendant une promo, le produit est seulement masqué des lectures catalogue, il n'y a pas de blocage serveur à l'écriture d'une vente.

## Produits digitaux (fichiers téléchargeables)

Le rôle **créateur** est **rouvert à l'inscription** (`Register.jsx` : radio « Créateur » actif — l'ancien blocage « Bientôt disponible » et la conversion créateur → vendeur ont été retirés ; l'ancien formulaire perdait aussi l'opérateur/numéro du créateur, désormais enregistrés dans `shop_payment_methods`). Rappel : le blocage global est **activé** (`membership_gate = "all"`) — les créateurs, comme les vendeurs et les boutiques, sont soumis à l'adhésion (vendeur 1 500 F, boutique/créateur 2 500 F / 30 jours).

Un produit **digital** (`products.is_digital = TRUE`) est un fichier que le client télécharge sur son appareil — **aucun fallback manuel** (pas d'envoi par email/WhatsApp).

- **Stockage** : bucket Supabase **PRIVÉ** `digital-products` (`SUPABASE_DIGITAL_BUCKET`), jamais d'URL publique. `server/storage.js` : `ensureDigitalBucket`, `uploadDigitalFile` (dédup par hash, sans conversion), `signedDigitalUrl(path, expiresSec)`, `digitalObjectExists`, `deleteDigitalFile`, `storageUsage(bucket)`.
- **Envoi (20 Mo)** : le navigateur téléverse le fichier **DIRECTEMENT** vers Supabase via une **URL d'upload signée** (`POST /api/digital/upload-url`, auth créateur/boutique ; hash SHA-256 calculé côté client, progression via XHR) puis n'envoie que `digital: { name, mime, size, key }` → `POST /api/products` / `PUT /api/products/:id` vérifie la présence et la taille de l'objet (`digitalObjectMeta`, HEAD) puis persiste `digital_path/digital_name/digital_mime/digital_size`. **Limite 20 Mo par fichier** (`DIGITAL_MAX_BYTES`, `storage.js`) — le corps Vercel (4,5 Mo) n'est plus impliqué. **Limite 2 produits digitaux par créateur** (`DIGITAL_MAX_PRODUCTS`, `products.js`, 403 `DIGITAL_PRODUCT_LIMIT`, appliquée aussi à la duplication) — **sauf comptes dispensés** (`DIGITAL_UNLIMITED_CREATORS`, ex. « bestrong » : nom ou email commençant par la valeur). **Quota par compte : réglé à 100 Mo en production** (env Vercel `DIGITAL_USER_QUOTA_MB`, min 50, défaut 500 ; somme des `digital_size` des produits du compte + nouveau fichier, 413 au-delà). Mode **legacy base64** (`digital.data`) conservé ≤ 3 Mo (`DIGITAL_INLINE_MAX_BYTES`) pour compatibilité. Clé contrainte à `users/{userId}/{sha256}/file.{ext}` (impossible de téléverser dans le dossier d'un autre compte ; même fichier re-téléversé = même clé = no-op `x-upsert`). Extensions autorisées : PDF, ZIP/RAR/7Z, EPUB/MOBI, Office, TXT/CSV/JSON/XML, MP3/M4A/WAV/OGG, MP4/WEBM/MOV, images. CSP : `connect-src` inclut `https://*.supabase.co` (`server/security.js` + `vercel.json`). UI : bouton « Téléversement… N % » dans `DigitalProductPicker.jsx`.
**Purge des orphelins** : `purgeDigitalOrphans()` (`server/cleanup.js`) supprime les objets du bucket non rattachés à un produit (uploads abandonnés) de plus de 24 h — exposée via `GET /api/admin/storage/usage` (consommation des 3 buckets + comptage des orphelins) et `POST /api/admin/storage/purge-digital-orphans` (purge effective, journalisée) ; carte « 💾 Stockage Supabase » de l'onglet ⚙️ **Système** (chargée au montage + 30 s comme le reste du panneau).

- **Téléchargement** : `POST /api/digital/:saleId/download` (+ `GET /api/digital/:saleId` pour l'état, `GET /api/digital/mine` pour le vendeur) → vérifie le droit (acheteur `sales.buyer_id`, **ou** code de confirmation pour un achat invité, **ou** propriétaire), puis renvoie une **URL signée 10 min** ; le client force l'enregistrement (`&download=<nom>`). Le fichier ne traverse **jamais** l'API (pas de limite 4,5 Mo, aucun egress Vercel). Chaque téléchargement d'acheteur est journalisé dans `digital_downloads` et compté (quota `products.digital_download_limit`, défaut 5).
- **Ouverture du droit** : `DIGITAL_REQUIRE_CONFIRMATION = true` (`server/routes/digital.js`) → téléchargement disponible dès que la **boutique confirme le paiement** (`shop_confirmed_at`, bouton « Confirmer ») ou que la vente est livrée. Passer la constante à `false` livre immédiatement à l'achat.
- **Achats SANS compte (invités)** : la preuve d'accès aux routes digitales (`/download`, `/video`, état, réconciliation) est le **code de confirmation** (`sales.confirm_code`, comparé par `isBuyerCaller`). `GET /api/sales/track/:id?` renvoie aussi les champs digitaux (`is_digital`, `digital_kind`, `digital_name`, `digital_mime`, `digital_size`) → la page **`/suivi`** affiche le téléchargement / la vidéo protégée dès qu'un code est fourni. Le tunnel (`DigitalBuyTunnel.jsx`) mémorise chaque vente créée dans `localStorage` (`mboppi_guest_digital_sales`, module `client/src/guestSales.js` — 30 entrées max, rien côté serveur) ; `/suivi` liste ces achats (« 🛒 Vos achats en ligne sur cet appareil ») avec suivi / suppression. Si l'invité perd son code, aucune récupération n'est possible (il doit contacter le vendeur).

- **Qui publie quoi (RÈGLE MÉTIER)** : un **créateur** publie **uniquement** des produits **digitaux** ; une **boutique** publie **uniquement** des produits **physiques** ; le **vendeur** ne publie rien mais **vend les deux** (via son code vendeur). Appliquée par le **serveur** (`products.js` POST/PUT/duplicate → 403 `CREATOR_DIGITAL_ONLY` / `DIGITAL_CREATOR_ONLY`) : l'interface ne peut pas la contourner. Côté UI, le sélecteur de fichier est **obligatoire et verrouillé** dans `CreatorDashboard.jsx` (plus de champs « quantité en stock » ni « frais de livraison » : mentions « Illimitée » / « Offerte ») et **retiré** de `ShopDashboard.jsx`.
- **Stock** : un produit digital ne réserve/décrémente **aucun** stock (`sales.js`, `orders.js`, `purchases.js` ignorent la réservation si `is_digital`) et la **quantité commandée n'est pas plafonnée** par `product.quantity` (`ProductDetail.jsx`, `store.jsx`, `ProductCard.jsx` → affichage « Disponibilité : illimitée »). `delivery_fee` est forcé à 0 et la catégorie par défaut est « Digital » (un créateur n'est pas rangé en « Arts & Artisanat » pour un fichier).
- **Édition/suppression** : remplacer le fichier supprime l'ancien objet s'il n'est plus référencé ; retirer le fichier est **refusé** si le produit a des ventes non annulées (les acheteurs doivent pouvoir retélécharger).
- **UI** : `DigitalProductPicker.jsx` (boutique + créateur), `DigitalDownload.jsx` (espace client + page d'achat), badge « 📁 Produit digital » sur la fiche produit.
- `digital_path` n'est **jamais** exposé par l'API produits (`productRow` le retire) ; `is_digital`, `digital_name`, `digital_size`, `digital_mime` sont publics.

## Contenus protégés — vidéos YouTube verrouillées (1.57.73)

Un produit digital peut être de type **`youtube`** (`products.digital_kind`) au lieu d'un fichier : le contenu est une **vidéo YouTube non répertoriée** dont l'identifiant ne quitte **jamais** le serveur avant validation du droit d'accès. Aucune URL publique, aucun fichier dans le Storage.

- **Création (créateur, `CreatorDashboard.jsx`)** : sélecteur « Type de contenu » — 📁 Fichier (téléversement 20 Mo, comportement historique, `digital_kind` NULL/`'file'`) ou 🎬 Vidéo YouTube protégée (`youtube_url` accepte `watch?v=`, `youtu.be/`, `embed/`, `shorts/`, `live/` ou l'ID 11 caractères → `youtube_id` en base), + champ **« Durée d'accès »** (`access_days`, vide = illimité).
- **Serveur** (`products.js`) : `parseYoutubeId` / `parseProtectedPayload` sur POST, PUT et duplication ; un produit `youtube` **n'exige aucun fichier** Storage (la garde « fichier obligatoire » ne vise que le type `file`) et n'entre pas dans le quota Mo. `youtube_id` n'est renvoyé qu'en `ownerView` (au propriétaire).
- **Déblocage** : `GET /api/digital/:saleId/video` (`server/routes/digital.js`) vérifie le droit comme le téléchargement — acheteur `sales.buyer_id`, **code de confirmation** pour un achat invité, ou propriétaire — puis l'état (`waiting_confirmation` si `shop_confirmed_at` absent, `access_revoked`, expiration `access_days` + `access_days_extra`) ; ce n'est qu'ensuite qu'il renvoie `embed_src` (`https://www.youtube-nocookie.com/embed/<id>?rel=0&modestbranding=1`). Journal `digital_downloads` (`action='video'`) : **ne consomme pas** le quota `products.digital_download_limit`.
- **Révocation / prolongation** : colonnes `sales.access_revoked` (bool) et `sales.access_days_extra` (int, jours ajoutés), créées par `initDb`. `GET /api/digital/product/:productId/accesses` (propriétaire) liste les acheteurs avec leur état ; `PATCH /api/digital/product/:productId/accesses/:saleId` accepte `{ revoked }` ou `{ extend_days }` (prolongation refusée si le produit est illimité).
- **UI acheteur** : `ProtectedVideo.jsx` (bouton « ▶ Regarder la vidéo » → vérification serveur → iframe `youtube-nocookie` ; messages « en attente de confirmation / accès révoqué / accès expiré / valable jusqu'au … ») monté dans `DigitalDownload.jsx` (espace client + page d'achat) et dans le tunnel `DigitalBuyTunnel.jsx` (`digital_kind` transmis par `store.jsx`, `Cart.jsx`, `PurchasePage.jsx` : la vidéo s'affiche dans la modale au lieu d'un téléchargement).
- **UI créateur** : `VideoAccessPanel.jsx` — acheteurs de chaque vidéo, boutons **+30/+90/+365 j** (désactivés si durée illimitée), **Révoquer / Rétablir**, compteur de visionnages 👁.
- **CSP** : `frame-src` autorise `https://www.youtube.com` et `https://www.youtube-nocookie.com` (`server/security.js` + `vercel.json`).
- ⚠️ **Limite assumée** : l'ID n'étant révélé qu'après validation serveur, le partage du lien est rendu difficile, mais une vidéo lue dans un navigateur reste enregistrable (capture d'écran). C'est un **contrôle d'accès serveur**, pas un DRM.

## SEO (SSR)

- `server/routes/seo.js` : lit `client/dist/index.html`, cache 30 s, `injectHead()` → title/description/canonical/og ; routes `/`, `/boutique/:id`, `/produit/:id`, `/sitemap.xml`. Base `https://mboppi-mboppi.vercel.app` (`PUBLIC_URL`).
- Produits sous promo **exclus** des listings SEO.

## Ventes / commandes

- `sales` : statuts `pending → confirmed (shop_confirmed_at) → delivered`, ou `cancelled`. `bought` existe dans la contrainte DB et l'UI mais **n'est jamais posé par le serveur**. Retraits « soft » via `hidden_for[]`.
- `purchases.js` : `POST /api/purchases` avec `product_id, seller_code (uppercasé), purchase_price (inutilisé), quantity, buyer_*, payment_method ('espece'|'mobile' ; 'automatic' accepté mais résidu iKeePay inutilisé)` ; auth **optionnelle**. Prix de référence = catalogue OU promo active. Achat authentifié avec `referred_by` → 2 %.
- `orders.js` : panier multi-articles, code de confirmation 6 caractères unique aussi contre `sales`, réservation atomique du stock, ordre enregistré si client authentifié.
  - ⚠️ Articles **sans code vendeur** : `seller_id = NULL` **mais** `commission` calculée (commission_percent) — non payable (`/:id/pay` refuse). À connaître avant toute évolution métier.
- **Livraison** (`POST /api/sales/:id/deliver`) : **livreur** ; `delivery_fee`, `payment_method` uniquement `espèce`/`espece`/`mobile`/`mobile_money` (**« en ligne » refusé**), `client_code` (le confirm_code), `shop_code` (boutique du produit) ; décrémente stock / `reserved_quantity` ; `signature` PNG optionnelle ; notifications. **Aucun paiement automatique**.
- **Paiement** : **manuel uniquement** ; plus de payin/webhook iKeePay ; `paySaleAutomatically()` = no-op de compatibilité.
- **Retraits/annulation** : `DELETE /api/sales/:id` (seller), `DELETE /api/sales/:id/referral` (parrain), `DELETE /api/sales/:id/delivered` (boutique/livreur) → `hidden_for`, bloqués si commissions impayées. `POST /api/sales/:id/cancel` (code client) restaure le stock. `PATCH /api/sales/:id/status` (shop) : `confirmed` = `shop_confirmed_at` seul (statut reste pending) ; `cancelled` libère le stock.
- **Facture PDF** : `client/src/components/Invoice.jsx` (jspdf 4.2.1 dynamique), boutons Shop/Seller/Livreur.

## Tableaux principaux (`server/db.js` `initDb`)

Créés par `initDb()` : `users`, `products`, `sales`, `offers`, `orders`, `push_subscriptions`, `newsletter_subscribers`, `seller_payment_methods`, `shop_payment_methods`, `livreur_payment_methods`, `notifications`, `reviews`, `audit_log`, `client_logs`, `admin_messages`, `admin_message_reads`, `wallet_accounts`, `wallet_transactions`, `automatic_payouts` (résidu), `payment_webhook_logs` (journal iKeePay — utilisé), `membership_payments` (adhésions en ligne — utilisé), `platform_payouts` (résidu), `donations`, `platform_settings` (mode de paiement, clés iKeePay, `ikeepay_webhook_secret`).

> ⚠️ `initDb()` **ne crée pas** `flash_promotions`, `item_views`, `daily_visits`, `activation_withdrawals`, `activation_withdrawal_items` : ces tables existent en base via des migrations externes. Sur une base neuve, les routes associées échoueraient tant qu'elles n'existent pas.
> Les **produits digitaux** sont, eux, créés par `initDb()` : colonnes `products.is_digital/digital_path/digital_name/digital_mime/digital_size/digital_version/digital_download_limit` + table `digital_downloads` (journal et compteur de téléchargements).

`purgeOldTransactions` : notifications supprimées après 90 jours ; ventes/commandes **jamais** purgées (historique financier). `server/cleanup.js` : purge stats > 6 mois + stock épuisé (dry-run possible).

## Autres fonctionnalités notables

- **Chat IA** (`server/routes/chat.js`) : Gemini (`GEMINI_MODEL`, fallbacks), max 2000 caractères, historique 12, injecte les produits en stock.
- **Robot WhatsApp** (assistant IA sur WhatsApp Cloud API) : `server/routes/whatsappBot.js` (`GET`/`POST /api/whatsapp/webhook`, monté **avant** `originCheck` dans `app.js` ; le POST répond `200 EVENT_RECEIVED` puis traite) + `server/services/whatsappBot.js` (même moteur `askAI` que le chat 💬, réglages `platform_settings` `wa_bot_enabled`/`wa_bot_greeting`/`wa_bot_fallback`/`wa_bot_system_prompt`, historique 12 par `wa_id`, anti-boucle, purge RAM > 500 sessions). Envoi via `sendWhatsAppText()` (`server/services/whatsapp.js`) qui **exige** `whatsapp_provider = cloud` + `whatsapp_cloud_token` + `whatsapp_cloud_phone_id` (mode `callmebot` ⇒ pas de robot). Prérequis : `WHATSAPP_VERIFY_TOKEN` (Vercel, repli `platform_settings.wa_bot_verify_token`) et `GEMINI_API_KEY` ; webhook `https://mboppi-mboppi.vercel.app/api/whatsapp/webhook` à déclarer chez Meta. Réglages admin : `GET/POST /api/admin/settings/whatsapp[-bot]`, UI carte « 🤖 Robot WhatsApp » (onglet ⚙️ Système) et « 📱 Notifications WhatsApp » (onglet 💰 Paiements). **Blocage production** : Meta n'autorise l'envoi vers n'importe quel numéro qu'après **vérification de l'entreprise** (Advanced Access `whatsapp_business_messaging`), pas après un « App Review » ; en mode développement le numéro de test gratuit suffit (5 destinataires, aucun document). Procédure complète + dossier de vérification (Cameroun) + alternatives : `WHATSAPP_META_VERIFICATION.md`.
- **Push** (`server/push.js`) : VAPID **uniquement** via env — **aucune clé par défaut** ; sans clés, push désactivés. Nettoyage 404/410. Consentement requis pour inscription shop.
- **Verone / Vitrine** (`server/routes/offers.js`, `server/routes/presentation.js`) : `GET /api/offers` public ; `POST /api/offers` et `DELETE /api/offers/:id` **non authentifiés** (état actuel — la protection documentée en V2 n'existe plus) ; `GET /api/offers/mine` renvoie toutes les offres. `pageRouter` → `/p`, `imageRouter` → `/api/img`. Pages Verone.jsx, VitrineOffre.jsx, OfferDetail.jsx.
- **Métriques** : `POST /api/metrics/views`, `POST /api/metrics/visit` (X-Visitor-Id), `GET /api/metrics/trending` (exclut les promos, cache s-maxage 120).
- **i18n** : toutes les traductions (fr/en/es/ar) dans `client/src/i18n.jsx` (`I18N = { fr: {}, en: {...EN, ...RICH_EN}, ar: {...AR, ...RICH_AR}, es: {...ES, ...RICH_ES} }`), clés françaises. `client/src/i18n/{en,es,ar}.js` supprimés (jamais importés). RTL pour ar.
- **PWA** : `client/public/sw.js`, cache `mboppi-v295`, app shell + API_SWR + push + 4 manifests.
- **Audit/sécurité** : `server/security.js`, rate limits, originCheck, CSP.
 - **Photos** : `server/storage.js` — buckets publics `photos` et `payment-proofs` ; clé `sb_secret_...` signée HS256 (`SUPABASE_JWT_SECRET`) ; fallback base64. `server/photo.js` : `{thumb, medium, large}`. **Conversion WebP automatique** : tout upload (produits, offres, preuves de paiement) passe par `toWebp()` (sharp, qualité 80, max 2000px, EXIF/rotate) — JPEG/PNG/HEIC convertis, GIF/AVIF/WebP conservés tels quels, micro-images < 8 ko et conversions « plus lourdes » ignorées ; en cas d'échec, l'image originale est conservée (aucune rupture d'upload).
- **Menu** (Navbar.jsx) : Produits, Créateurs, Je soutiens, Formations et Digital (chariow.pics), Tutoriel Mboppi (TikTok @mboppishop), espaces par rôle, **Générateur de documents (créateur, `/generateur`)**, Administration 🛡️.
- **Livreurs** : `server/routes/livreurs.js` (`GET /api/livreurs`, `/options`) **non monté dans app.js** et `api.listLivreurs` absent côté client → page `/shop/livreurs` non fonctionnelle (état actuel).
- **Donations** : en mode **manuel**, `POST /api/donations` enregistre un don déclaratif (virement direct hors plateforme, validation par l'équipe). En mode **automatique**, `POST /api/payments/donation-payin` ouvre le checkout iKeePay et le webhook marque le don `completed`. Le suivi admin des paiements en ligne est sur `GET /api/admin/payments`.

## Conventions de dev (IMPORTANT)

1. **Ne jamais committer sans demande explicite.** Quand le user demande « deployer » / « mettre en ligne » : bump + commit + push.
2. **Bump de version à chaque déploiement** : `client/package.json` + `client/package-lock.json` (lignes 3 **et** 9, ne pas toucher les entrées deps `loose-envify@1.8.3` / `update-browserslist-db@1.8.3`) + `package.json` racine. PWA : `client/public/sw.js` CACHE_NAME `mboppi-vXXX` incrémenté. État actuel : **1.57.64 / mboppi-v295**.
3. **Build** : `npm run build` dans `client/` (le hash du JS local diffère de celui de Vercel pour des raisons d'environnement ; vérifier le déploiement via le CSS hash ou en cherchant une chaîne caractéristique du nouveau code dans le JS servi).
4. **Vérifier le déploiement** : attendre ~75–90 s après push, puis `curl` sur `https://mboppi-mboppi.vercel.app/` (header `Accept: text/html` pour le HTML SEO) et chercher le hash CSS/JS du build local ; tester les API concernées.
5. Commandes utiles : `node --check server/routes/*.js` pour la syntaxe serveur.
6. Lignes de commande Windows : PowerShell — ne pas utiliser `&&`, utiliser `;` / `if ($?)`. Test-Path avant de créer des dossiers. `curl.exe` (pas l'alias PowerShell).
7. Le produit sous promo est invisible SAUF via son lien direct `/produit/:id` (landing promo) — c'est voulu.

### Bascule vers un domaine personnalisé (déjà préparée — 17/09/2026)

Le code est **piloté par l'environnement** : changer de domaine ne demande **aucune modification de code**.

1. Acheter le domaine puis Vercel → *Settings → Domains* → **Add** ; créer les enregistrements affichés (`A @ → 76.76.21.21`, `CNAME www → cname.vercel-dns.com`) ; attendre « Valid Configuration » (SSL automatique).
2. Vercel → *Settings → Environment Variables* (Production) : `VITE_SITE_URL`, `SITE_URL`, `PUBLIC_URL` = `https://mondomaine` (+ `ALLOWED_ORIGIN` si utilisé) → **Redeploy**. ⚠️ `SITE_URL` est marquée **Sensitive** (valeur illisible) : l'écraser via `vercel env rm SITE_URL production` puis `vercel env add SITE_URL production` (ou directement dans le dashboard).
3. Vérifier : `curl -s https://mondomaine/ -H "Accept: text/html"` doit montrer `canonical`/`og:url` sur le nouveau domaine ; `/sitemap.xml` idem.
4. Garder l'ancienne adresse en **redirection 301** (Vercel → *Domains → Edit → Redirect to*) pour ne casser aucun lien déjà partagé.
5. Hors code : mettre à jour l'URL du webhook chez **Meta** (WhatsApp), re-soumettre le **sitemap** à Google, et le profil **Trustpilot** (le lien `Footer.jsx` + le meta `trustpilot-one-time-domain-verification-id` sont liés au domaine).

Pilotés par l'environnement : `client/src/config.js` (`BASE_URL` ← `VITE_SITE_URL`), `client/vite.config.js` (remplace `__SITE_URL__` dans `index.html`), `client/index.html`, `client/src/components/{ShareVitrine,Invoice}.jsx`, `client/src/pages/Admin.jsx`, `server/app.js` + `server/security.js` (CORS ← `SITE_URL`/`PUBLIC_URL`), `server/mailer.js`, `server/chat-knowledge.js`, `server/routes/{seo,auth,newsletter,admin}.js`, `server/services/campaigns.js`.
Restent en dur **volontairement** : `client/src/components/Footer.jsx` (lien Trustpilot, attaché au profil) et `.github/workflows/keepalive.yml` (ping sur l'alias `vercel.app`, toujours valide).

## Historique récent des modifications

(Changelog partiel — version courante **1.57.81** / cache PWA **mboppi-v312**. Les entrées ci-dessous s'arrêtent à 1.52.2 ; les versions 1.57.x sont résumées en fin de liste.)

- **1.57.77** : **Studio d'édition page par page (`DocStudio.jsx`, « Digital Publishing Studio ») + i18n complet** — nouvel onglet **Studio** du Générateur (`client/src/generator/DocStudio.jsx`, `StudioCanvas.jsx`, `studioModel.js`, `studioLayouts.js`, `studioAi.js`, `studioExport.js`) : pages dérivées du document (`gen_documents.page_layout`, JSONB), édition directe des textes, ajout/duplication/suppression/réorganisation de pages et d'éléments, calques, alignements, guides magnétiques, rotation, undo/redo, zoom, exports **PDF/EPUB du Studio** (`exportStudioPdf`/`exportStudioEpub`), publication produit digital ; traductions fr/en/ar/es (blocs `GEN3_*`→`GEN5_*`) ; **correctif de syntaxe `i18n.jsx` ligne 6023** (entrée malformée) qui cassait le build.

- **1.57.78 / 1.57.79** : **crash du Studio à l'ouverture** — `Cannot access 'Lt' before initialization` : `patchEls` était utilisé dans le tableau de dépendances d'`onImagePicked` **avant sa déclaration** (TDZ) ; il est désormais déclaré en amont (`DocStudio.jsx`). Puis **React #31** (objet `describePage` rendu comme libellé de miniature de page) corrigé dans les miniatures de pages.

- **1.57.80** : **Générateur — synchronisation réelle des 4 onglets (Contenu / Design / Studio / Aperçu) + décor au-dessus du texte corrigé + bouton redondant retiré** — **une seule source de vérité par document** : (1) `studioModel.js` expose `studioDesignKey(docMeta)` (modèle, `style_overrides`, format, marges, sommaire) et `studioContentKey(html)` (empreinte FNV-1a du texte), enregistrées dans l'enveloppe `page_layout` à chaque sauvegarde du Studio ; à la réouverture, si l'une des deux a changé, les pages du Studio sont **reconstruites automatiquement** (message « Studio synchronisé… » + autosave) au lieu de rester figées sur l'ancien texte/modèle ; (2) le **parent régénère** la mise en page Studio (`resyncStudio`) dès qu'une frappe (debounce 800 ms) ou qu'un réglage de design change, et à l'ouverture d'un document périmé — l'enregistrement suit ; (3) l'onglet **Aperçu** affiche désormais **les pages du Studio** (`StudioCanvas` en lecture seule, échelle `GEN_PREVIEW_SCALE × fitScale`) dès qu'une mise en page existe, avec bandeau « Mise en page du Studio active… » et bouton « Ouvrir le Studio » ; (4) `doExport` exporte **le PDF depuis les pages du Studio** quand elles existent (aperçu = PDF) au lieu de repaginer automatiquement ; (5) **décor masquant le texte** : sur la page **Sommaire** le conteneur de texte n'était pas positionné et peignait donc **sous** le SVG absolu du décor — `position: relative; z-index: 2` ajoutés ; (6) le bouton **« ➕ Nouveau document »** (redondant : le formulaire de création est en bas de la bibliothèque) est retiré, le formulaire devient toujours visible, état `creating` supprimé. Traductions fr/en/ar/es des 3 nouvelles clés. Cache PWA v311.

- **1.57.81** : **Générateur — réglages Design qui « changeaient tout seuls » (cause trouvée) + décor du sommaire** — **(1) réponses de sauvegarde périmées** : `saveNow` appliquait `setMeta((cur) => ({ ...cur, ...d.document }))` — avec deux PATCH en vol (ex. changement de police puis de taille), la réponse **la plus ancienne** pouvait arriver en dernier et **remettre l'ancien design** (police, couleurs, modèle) : les réglages « ne collaient pas » puis « changeaient seuls ». Un **compteur de séquence** (`saveSeq`) ignore désormais toute réponse périmée, et `page_layout` (non envoyé par cette route) n'est plus repris de la réponse — le Studio n'est donc plus écrasé par un autosave de contenu. **(2) mise en page du Studio perdue** : la resynchronisation (`resyncStudio`) posait `page_layout` localement puis appelait l'autosave de contenu, qui ne transporte pas ce champ → l'ancienne mise en page revenait à la réponse suivante ; elle est désormais **enregistrée directement** via `api.genSaveDocument(doc.id, { page_layout })`. **(3) décor au-dessus du texte** : dans `exportPdf.js`, `drawToc` **repeignait la page entière** (fond opaque) APRÈS les décors → le sommaire PDF perdait son décor (et l'ordre décor/texte variait selon les pages) ; le fond est maintenant peint **avant** les décors dans la branche `toc` (`drawCoverDecorPdf` puis `drawPageDecor` puis `drawToc`), donc décor **derrière** le texte partout. **(4) hiérarchie garantie** : nouveau module partagé `client/src/generator/PageDecor.jsx` (décor de page : toprule, topbar, headerband, bottomband, sidestrip, frame, doublerule, noterule, sidebartint, doubleband, sideline, masthead) utilisé par l'aperçu **ET** le Studio (`StudioCanvas`), tous les calques du décor en `z-index: 0`, conteneurs de texte en `z-index: 1`/2, `CoverDecor` en `z-index: 0`, `.gen-cover-body`/`.gen-cover-author` en `z-index: 1` — le Studio affiche désormais le fond et le décor du modèle (`page.design.bg`/`decor`) exactement comme le PDF. Cache PWA v312.

- **1.57.75** : **Générateur — bibliothèque de modèles étendue + IA livre (phase 1 du moteur de design)** — la bibliothèque passe de **12 à 20 modèles** (nouveaux : Corporate, Catalogue, Roman, Spirituel, Santé & Bien-être, Cuisine, Voyage, Académique — uniquement des valeurs déjà supportées par les 3 rendus, aucun moteur modifié) avec **métadonnées** (catégorie, style, audience, secteur via `templateMeta`/`templateFacets`) ; **filtres de bibliothèque** dans l'onglet Design : sélection Catégorie / Style, recherche texte (nom + métadonnées), **favoris ⭐** (localStorage `gen_fav_templates`) et rangée **« 🕘 Récents »** (`gen_recent_templates`, mémorisés à l'application d'un modèle) ; **« 🎲 Générer une variante »** : variation harmonieuse du modèle actif (rotation de teinte HSL des couleurs, pairage de polices alternatif, couverture assortie) appliquée via `style_overrides` — contenu intact, réversible par Styles avancés (`templateVariant`) ; **rapport « 🔍 Analyse du document »** après import DOCX/TXT/MD (chapitres h1, sections h2, images, tableaux, mots, pages estimées ~300 mots/page) avec bouton « Choisir un design » ; **IA Text-to-Book** : deux nouvelles actions `book_plan` (plan complet d'ebook à partir d'une consigne — introduit, 6-12 chapitres avec sous-sections résumées, conclusion — converti en document structuré par le détecteur) et `write_chapter` (rédaction d'un chapitre entier à partir de son titre), ajoutées à `AI_TEMPLATE_IDS` + garde-fous serveur inchangés ; compteur de modèles dynamique dans la suggestion de design. Traductions fr/en/ar/es (bloc `GEN2_*`). Cache PWA v306.

- **1.57.76** : **Générateur — recommandation intelligente + commandes naturelles + aperçu réel des modèles** — nouveau **Smart Template Recommender** (`recommendTemplates()` exporté de `client/src/generator/designCommands.js`) qui analyse le contenu, le secteur, le public, la longueur et le ton pour proposer 5 modèles adaptés (items 16/65) ; **boîte de commandes de design** (`client/src/generator/designCommands.js`, 370 lignes) qui interprète des phrases libres comme *« utilise un style finance »*, *« change la couverture en sombre »* ou *« ajoute un watermark »* et applique les modifications via `style_overrides` sans toucher au contenu (items 45-56) ; **aperçu réel d'un modèle** (bouton 👁 sur chaque carte → modale paginée du document actuel) avec bouton « Utiliser ce modèle » créant une version non destructive (items 17/18/62). 2 nouvelles actions IA serveur (`book_plan`, `write_chapter`) ajoutées à `AI_TEMPLATE_IDS`. Traductions fr/en/ar/es (blocs `GEN3_*`, `GEN4_*`, 136 clés). **Correctifs du même lot** : `busy` hors portée dans la vue Design (crash `ReferenceError: busy is not defined` à l'ouverture de l'onglet Design) retiré, `coverIdeas` (propositions de couverture) déclaré dans `parseDesignCommand`, `editingDigital` rétabli dans `CreatorDashboard` (désactivation du contenu digital) — contrôle de portée automatique sur tout `client/src` : aucune variable hors portée. Cache PWA v307.

- **1.57.74** : **accès des acheteurs SANS compte à leurs produits digitaux** — la preuve d'accès aux routes digitales (`/download`, `/video`, état, réconciliation) reste le **code de confirmation** (`sales.confirm_code` via `isBuyerCaller`) ; deux trous comblés : **①** `GET /api/sales/track/:id?` renvoie désormais les champs digitaux (`is_digital`, `digital_kind`, `digital_name`, `digital_mime`, `digital_size` — jamais `youtube_id` ni `digital_path`) et la page **`/suivi`** affiche le téléchargement (fichier + tampon PDF) ou la lecture de la vidéo protégée dès qu'un code est fourni (`DigitalDownload.jsx` monté dans `Suivi.jsx`, s'efface seul pour un produit physique) ; **②** le tunnel digital (`DigitalBuyTunnel.jsx`) mémorise chaque vente créée dans `localStorage` (`mboppi_guest_digital_sales`, nouveau module `client/src/guestSales.js` — 30 entrées max, rien côté serveur, mémorisation dès la création même avant paiement) ; `/suivi` liste ces achats (« 🛒 Vos achats en ligne sur cet appareil ») avec boutons **Suivre** (recharge la vente + son code → contenu accessible) et suppression. Traductions fr/en/ar/es (blocs `GUEST_*`). ⚠️ Seul cas irrécupérable : invité ayant perdu son code **et** vidé son stockage navigateur → contact vendeur. Cache PWA v305.

- **1.57.73** : **contenus protégés — vidéos YouTube verrouillées par achat** — un produit digital peut désormais être de type **`youtube`** (`products.digital_kind` = `'file' | 'youtube'`) : dans son espace, le créateur colle un **lien YouTube** au lieu d'un fichier, choisit la **durée d'accès** (30 / 90 / 365 jours ou illimité) et le **nombre d'appareils** autorisés. L'ID de la vidéo n'est **jamais** exposé dans les réponses publiques de l'API (`productRow` le retire comme `digital_path`) : il n'est révélé qu'après vérification **côté serveur** du droit d'accès via `GET /api/digital/:saleId/video`, qui contrôle l'authentification (acheteur `sales.buyer_id`, **ou** code de confirmation pour un achat invité), la **confirmation du paiement** (`DIGITAL_REQUIRE_CONFIRMATION` : `shop_confirmed_at` ou vente livrée), la **révocation** (`sales.access_revoked`) et l'**expiration** (`sales.access_days_extra` + date d'achat) ; la réponse est minimale (`{ embed_src, title, expires_at }`) → iframe `youtube-nocookie.com` avec `referrerPolicy="strict-origin-when-cross-origin"` et `allowFullScreen`, **aucun téléchargement** (bouton « ▶ Regarder la vidéo » au lieu de « Télécharger »). Nouveaux endpoints : `GET /api/digital/product/:id/accesses` (créateur/boutique propriétaire : acheteurs, expiration, appareils, statut), `POST /api/digital/access/:saleId/revoke` et `.../prolong` (prolongation +30/+90/+365 jours). UI : `ProtectedVideo.jsx` (espace client, page d'achat, tunnel de paiement) et `VideoAccessPanel.jsx` (panneau créateur « 🔒 Accès aux vidéos protégées » : liste des acheteurs, révocation / réactivation, prolongation) ; CSP `frame-src` ouverte à `https://www.youtube.com` et `https://www.youtube-nocookie.com` (`server/security.js` + `vercel.json`). Le mode **fichier** (bucket privé + URL signée 10 min) reste le défaut et la protection la plus forte. ⚠️ **Limite assumée et documentée** : YouTube n'offre pas de contrôle d'accès par vidéo — l'ID ne circule qu'après validation serveur (le partage du lien est donc rendu difficile), mais aucun contenu vidéo web ne peut être protégé à 100 % (capture d'écran / réenregistrement). Cache PWA v304.

- **1.57.72 (correctif TDZ)** : l'aperçu plantait à l'ouverture d'un document (`ReferenceError: Cannot access 'w' before initialization` — `computeFit` évaluait `preview` dans son tableau de dépendances AVANT la déclaration `const [preview, setPreview]` ; bloc déplacé après les déclarations d'état) ; entrée initiale : **couverture — décor derrière l'image + aperçu adapté au téléphone** — dans les 3 rendus (aperçu `Generator.jsx`, PDF `exportPdf.js`, miniature `coverImage.js`) le décor géométrique est désormais dessiné **AVANT** l'image de fond : l'image reste entièrement visible devant les formes (avec photo, le décor reste présent sur les pages intérieures) ; **aperçu responsive** : nouveau facteur d'ajustement `fitScale` (multiplie `GEN_PREVIEW_SCALE` dans `GenPage`) calculé d'après la largeur réelle de la zone d'aperçu et la largeur de page paginée (`preview.box.w`, paysage inclus) — sur téléphone la page entière tient dans la largeur (recalcul au montage, via `requestAnimationFrame`, au `resize` et quand l'aperçu est reconstruit ; plancher 0,3 ; le PDF reste au format exact, seul l'affichage est rétréci) ; cache PWA v302.

- **1.57.70** : **décor géométrique sur TOUTES les pages du Générateur, derrière le texte** — les primitives du modèle (`coverShapeSpec`) sont désormais dessinées sur chaque page (couverture, copyright, table des matières, contenu) dans les 2 rendus : aperçu HTML (`Generator.jsx`, `CoverDecor` en premier enfant de chaque `.gen-page`) et PDF (`exportPdf.js` : copyright reçoit `decorPrims` dessinées après le fond, toc et contenu les dessinent après le fond/avant le texte — jamais par-dessus le texte) ; cache PWA v301.

- **1.57.69** : **décor géométrique des couvertures du Générateur (3 rendus) + badge « X en attente » retiré des cartes produit** — nouveau moteur de décor partagé `client/src/generator/coverDecor.js` (convertit `coverShapeSpec` de `templates.js` en primitives `poly`/`rect`/`circle`/`ring`) dessiné par **`CoverDecor.jsx`** (SVG, aperçu HTML), `drawCoverDecorCanvas` (miniature + couverture produit, `coverImage.js`) et `drawCoverDecorPdf` (jsPDF, `exportPdf.js`) — aperçu = PDF = miniature à l'identique ; formes : secteur+anneaux (arc : Moderne, Motivation), disque débordant (circle : Technologie, Jeunesse), coin biseauté (diag : Business, Finance, Magazine), barres d'accent (bars : Professionnel, Éducation — `coverShape: "half"` n'avait **aucun case** et retombait sur aucun décor, renommé `bars`) ; formes dessinées APRÈS l'image de fond et AVANT le texte (titre toujours lisible, aucune couche de couleur pleine page) ; cartes produit (`ProductCard.jsx`) : la pastille « X en attente » est supprimée, seul « X vendus » s'affiche ; cache PWA v300.

- **1.57.68** : **couverture du Générateur sans couche de couleur + texte du bandeau de paiement raccourci** — mise en page « bande » (modèles Moderne, Business, Motivation, Jeunesse) : le **dégradé de la couleur d'accent sur le tiers inférieur de la photo de couverture est supprimé dans les 3 rendus** (aperçu `Generator.jsx`, PDF `exportPdf.js`, miniature produit `coverImage.js` — imports `withAlpha` devenus inutiles retirés) ; la photo reste entièrement visible et nette, le titre blanc se pose directement sur l'image, le réglage manuel « Opacité de l'image » continue de fonctionner ; bandeau `PaymentMethodsStrip` : mention « des pays couverts par iKeepay » retirée (« Carte bancaire (Visa / Mastercard), USDT et Mobile Money — transaction chiffrée et vérifiée. », traductions en/es/ar mises à jour) ; cache PWA v299.

- **1.57.67** : **bandeau défilant des moyens de paiement + notifications de vente digitale uniquement après paiement** — `PaymentMethodsStrip` devient un bandeau **défilant** (droite → gauche, une ligne, boucle sans à-coup, pause au survol, `prefers-reduced-motion` → statique) et est ajouté sur l'achat direct (fiche produit, sous « Télécharger ») et dans le panier (bloc produits digitaux, en plus de la page `/acheter`) ; panier : « Les frais de livraison sont confirmés avec la boutique. » masqué si le panier ne contient que du digital, texte digital raccourci (« 📁 Produits digitaux : paiement en ligne immédiat, puis téléchargement automatique du fichier. », traductions en/es/ar) ; espace compte : mention « JPG, PNG ou WebP — convertie automatiquement… » de la photo de profil supprimée ; **correction notifications fantômes** : `POST /api/payments/digital-payin` n'insère plus cloche ni push (un simple clic sur « Télécharger » sans paiement ne sonne plus) — cloche `sale_order` + push créateur/vendeur déplacés dans `settleDigitalSale()` (webhook iKeePay ou réconciliation du sondage) ; cache PWA v298.

- **1.57.66** : **tunnel d'achat digital unifié (iKeePay) + étiquette « Créateur » sur les produits digitaux** — nouveau composant `client/src/components/DigitalBuyTunnel.jsx` partagé par les 3 portes d'entrée : fiche produit (`/produit/:id`, flux existant conservé), **lien vendeur `/acheter/:id?code=XXX`** (PurchasePage : plus aucun formulaire pour un digital — le bouton « Télécharger » ouvre directement le tunnel, code vendeur transmis pour la commission) et **panier** (Cart : les produits digitaux sont séparés des articles physiques — bloc « 📁 Produits digitaux » avec bouton « Payer et télécharger », aucun formulaire de livraison ; les articles physiques gardent le formulaire existant). Flux identique partout : `POST /api/payments/digital-payin` → checkout iKeePay inline → sondage `wait-online` (4 s, réconciliation webhook automatique) → **téléchargement automatique** du fichier (URL signée, un par un si plusieurs) → félicitations ; boutons de secours si le navigateur bloque le téléchargement auto. `store.jsx` : `is_digital` enregistré dans le panier + `setItemDigital` (rattrapage des anciens paniers). Étiquette « Créateur : X » (lien `/createur/:id`, badge « Compte vérifié ») appliquée dès que `product.is_digital` — plus seulement quand `shop_role === "creator"` (cartes, fiche produit, page d'achat) ; correctif CSS résiduel (doublon `.gen-pages`) ; cache PWA v297.

- **1.57.54** : **limites des produits digitaux resserrées** — taille maximale par fichier ramenée à **20 Mo** (route `upload-url`, validation produit, validateur zod, sélecteur client) et **2 produits digitaux maximum par créateur** (`POST /api/products` et `/duplicate`, 403 `DIGITAL_PRODUCT_LIMIT`), **sauf le compte « bestrong »** (`DIGITAL_UNLIMITED_CREATORS`, dispensé des deux limites) ; cache PWA v285.
- **1.57.55** : **documentation alignée sur l'adhésion obligatoire pour tous** — le blocage global (`membership_gate = "all"`) est bien la valeur active en production : README (nouvelle section adhésion), CGU (`2.1. Adhésion et sécurité`), CGV (adhésion + téléchargement des produits digitaux dans « 3. Paiement et livraison »), FAQ (« Qui doit payer l'adhésion ? » + deux nouvelles entrées « Qui peut publier des produits ? » et « Comment télécharger un produit digital ? »), connaissances de Vérone (`chat-knowledge.js`, fr/en/ar), libellés admin et commentaires de code mis à jour (vendeur 1 500 XAF, boutique et créateur 2 500 XAF / 30 jours ; clients et livreurs sans frais) ; traductions en/ar/es des nouvelles clés ; cache PWA v286.

- **1.57.56** : **liens cliquables dans le chat + délai de paiement des retraits** — les réponses de l'assistante Vérone (`ChatWidget.jsx`) rendent désormais cliquables les liens qu'elle donne : les URL externes (`http(s)://`) s'ouvrent dans un nouvel onglet, les chemins internes (`/boutique/…`, `/produit/…`, `/faq`, `/soutien`…) deviennent de vrais liens SPA (`.chat-link` dans `styles.css`) ; **mention « paiement sous 72 h maximum »** ajoutée sur les retraits de gains (vendeur, créateur) — carte « Gains en ligne » (`OnlineEarningsCard.jsx`, vendeur + créateur), modale de retrait du tableau de bord vendeur (`SellerDashboard.jsx`) et section retraits du tableau de bord créateur — plus la FAQ (« Comment recevoir mes gains ? ») et les connaissances de Vérone (fr/en/ar) ; traductions en/ar/es ; cache PWA v287.

- **1.57.57** : **onglet Générateur de documents (ebooks/PDF) dans le panneau Admin** — nouveau module isolé (tables additives `gen_documents`, `gen_documents_data`, `gen_versions` dans `initDb`, route `server/routes/generator.js` montée sur `/api/generator` avec garde `authRequired` + `roleRequired("admin")`, limite 3,5 Mo, anti-injection sur `href`/`src`, audit `generator.*`) : bibliothèque de documents (référence unique `DOC-<année>-<8hex>`, statut brouillon/prêt, duplication, versions), éditeur TipTap 3 (titres, listes, citations, liens, tableaux, images dataURL compressées, alignements), **import TXT/Markdown** avec détection automatique de structure (`client/src/generator/structure.js` : chapitres, listes, citations) et **import DOCX** (mammoth), **14 modèles de design** (`templates.js`), formats A4/A5/Letter/ebook + orientation + marges, couverture personnalisable (titre, sous-titre, couleurs, image de fond), **pagination moteur navigateur** (mesure mot à mot via l'API Range, veuves/orphelines, titres insécables, table des matières à numéros exacts — `paginate.js`), **aperçu fidèle** du PDF (`Generator.jsx`), **export PDF réel jsPDF** (`exportPdf.js` : métadonnées complètes, QR code de vérification, filigrane, en-têtes/pieds, page de copyright) ; autosave débouncé 1,5 s + instantanés de version ; dépendances ajoutées `@tiptap/*` 3.31.3, `mammoth`, `qrcode` ; onglet 📚 entre Produits et Système ; cache PWA v288.

- **1.57.58** : **Générateur — assistant IA, contrôle qualité, EPUB, publication produit digital, versions restaurables, page de vérification** — **assistant IA (Gemini)** via `POST /api/generator/ai` (réutilise `askAI` de `chat.js`, 503 clair sans `GEMINI_API_KEY`) : 11 actions (structurer, améliorer/corriger/reformuler/résumer/développer/changer le ton/traduire, quatrième de couverture, biographie, proposer un design → template + couleurs appliqués) ; l'IA propose, l'utilisateur valide avant insertion ; **🔍 DOCUMENT CHECK** (`client/src/generator/check.js`) : score descriptif (structure/lisibilité/images/pagination/couverture), erreurs/avertissements/suggestions + boutons « Corriger » (TOC, copyright, QR, couverture, marges compactes, justification) ; **export EPUB 3** (`client/src/generator/epub.js`, JSZip : XHTML par chapitre, nav + toc.ncx, couverture, métadonnées, styles du modèle) ; **publication « Vendre sur Mboppi »** : PDF exporté en mémoire → hash SHA-256 → URL signée → PUT direct Supabase (bucket privé `digital-products`) → `POST /api/generator/documents/:id/publish` crée/met à jour le produit digital (prix XAF, description, couverture stockée comme photo produit, lien `/produit/:id`) ; **versions restaurables** (`POST .../versions/:vid/restore` + panneau dépliable dans l'éditeur) ; **page publique de vérification** `/verifier` et `/verifier/:ref` (`VerifyDoc.jsx`, route publique `GET /api/generator/verify/:ref` — référence, auteur, date, empreinte tronquée) ; **styles personnalisés** (`style_overrides` — champ existant, désormais appliqué par `resolveTemplate`) ; **miniatures** dans la bibliothèque (`coverImage.js` : `renderCoverImage`, `libraryThumb`) ; QR code pointant vers la page de vérification ; dépendance ajoutée `jszip` ; cache PWA v289.

- **1.57.59** : **Générateur — correctifs et UX éditeur** — **insertion d'image réparée** : l'import TipTap `Image` masquait le constructeur natif (`new Image()` sur un objet `Node.create` → TypeError → « Image non lisible ») ; import renommé `ImageExtension` + `new window.Image()` partout (`Generator.jsx`, `paginate.js`, `coverImage.js`) ; **PDF : plus de texte compacté/superposé** — chaque mot est centré sur son point milieu mesuré avec la largeur **calculée par jsPDF** (`getTextWidth`), l'écart de métrique entre polices navigateur et PDF ne s'accumule plus, traits de soulignement/barré à la largeur réellement posée ; `await document.fonts.ready` avant la mesure de pagination ; **erreurs 500 résolues sur l'assistant IA et la publication** : `generator.js` utilisait `err.status` alors que le gestionnaire global lit `err.statusCode` (13 occurrences corrigées + repli `err.status` dans le handler `app.js`) — les messages 4xx (« Sélectionnez d'abord un passage », prix invalide…) s'affichent enfin correctement ; **publication avec l'admin virtuel (id 0) réparée** : `products.shop_id REFERENCES users(id)` rejetait l'INSERT → nouveau `resolveOwnerId` (id réel, sinon compte admin réel, sinon 422 explicite) appliqué à upload-url/publish (clé, couverture, INSERT/UPDATE) ; `logAudit` enregistre `user_id NULL` (au lieu de 0) pour l'admin virtuel — la trace d'audit n'est plus perdue ; **bouton 🔍 Vérifier** (barre du haut + « Contrôle qualité » dans l'aperçu) — le rapport DOCUMENT CHECK existait sans déclencheur ; **panneau ⚙️ Styles avancés** dans la vue Design (polices corps/titres, alignement, interligne, espacement, 6 tailles, 4 couleurs, réinitialisation) persisté via `style_overrides` (champ manquant à l'autosave ajouté) ; **ruban statique** : barres d'en-tête regroupées dans `.gen-headwrap` collant sous la navbar (`--header-height`/`--header-height-lg`) et **défilement interne de la zone de texte** (`.tiptap` max-height + overflow-y + overscroll contain) ; cache PWA v290.
- **1.57.60** : **Générateur — lisibilité en mode sombre + grilles mobiles** — **boutons de l'assistant IA enfin visibles en thème sombre** : ils réutilisaient la classe globale `.btn-outline` (fond transparent + bordure `--border` `#2a3544`) posée sur une carte `#1a222c` → contraste quasi nul ; ils ont désormais un style « chip » thémable (`.gen-ai-actions .btn` : fond `--input-bg`, bordure `--border-strong`, texte `--text`) avec survol `--primary` en clair et `--primary-light` en sombre ; **audit complet du module** : toutes les couleurs en dur (`rgba(0,0,0,…)`, `#f4f4f5`, `.gen-preview-zone`, `.gen-form`, `.gen-design-block`, `.gen-toolbar`, bordures `.tiptap`, `.gen-versions li`, `.gen-check-report`, `.gen-save`, `.gen-quality`, `.gen-template-card.active`, `.hint`, `.gen-mini-label`, `.gen-published-ok`) remplacées par les variables de thème (`--card`, `--card-hover`, `--border`, `--border-strong`, `--text`, `--text-secondary`, `--text-muted`, `--muted`, `--soft`, `--soft-2`, `--input-bg`, `--primary-light`) déclarées à la fois dans `:root` et `[data-theme="dark"]` ; l'intérieur de l'éditeur (`.tiptap` : page blanche, tableaux, `pre`, blockquote) reste volontairement clair — c'est la « feuille de papier », identique au PDF ; **grilles mobiles (≤ 640 px)** : modèles de design **4 par ligne** (`repeat(4, minmax(0,1fr))`, pastilles 38 px), Format et mise en page **2 par ligne**, Couverture **2 par ligne** (image de fond pleine largeur), Styles avancés **2 par ligne** (`.gen-style-grid` + `.gen-form-row`), assistant IA **2 boutons par ligne** avec libellé pleine largeur ; desktop inchangé (modèles en `auto-fill minmax(88px,1fr)` — grille régulière au lieu du flex-wrap irrégulier) ; cache PWA v291.
- **1.57.61** : **Générateur — géométrie de l'aperçu et du PDF corrigée (texte superposé, marges, tableaux)** — la position verticale mesurée d'une ligne restait **relative au bloc** au lieu d'être ramenée dans la boîte de son atome : toutes les 3ᵉ lignes se peignaient en haut de page (texte compacté/superposé, aperçu **et** PDF). Désormais `item.top` est la position absolue dans la boîte de contenu (`paginate.js` : un atome = une ligne, `lines[0] = { top: 0, bottom: hauteur }`) ; l'aperçu peint `item.top + ln.top` (`Generator.jsx`, composant `GenLine` avec prop `top`) et le PDF ajoute en plus la marge de page (`m.left`/`m.top`) pour le texte (`drawLine(doc, ln, offsetX, offsetY)`), les images, les séparateurs **et** les lignes de tableau (`drawTableRow(..., box)` avec offsets cellule/ligne) ; **cellules de tableau mesurées dans le repère de leur cellule** (`collectLines(td, { left, top })`) — le texte n'est plus décalé à la position du tableau ; **veuves/orphelines : les lignes reportées ne sont plus perdues** (tampon `carry` + `restack` dans `flowAtoms` — l'ancienne coupure retirait la ligne de la page sans la replacer, du texte disparaissait à chaque page) ; `check.js` (`itemBottom` = `item.top + ln.bottom`, plus de fausses alertes de débordement) ; **aperçu à l'échelle réelle** (conversions `PX_PER_MM`/`PT_TO_PX` avant le zoom 75 %, `.gen-preview-zone` en `overflow-x: auto` au lieu de rogner la page, `.gen-pages` aligné à gauche sur mobile) ; validé par un test Node de géométrie (6 pages, 1494/1494 mots relus dans le PDF produit, lignes distinctes et espacées, 12 cellules de tableau confinées, marges respectées) ; cache PWA v292.

- **1.57.63** : **Générateur — cloisonnement par compte renforcé + l'aperçu suit enfin le modèle de design** — **fuite admin → créateur corrigée** : `generatorRequest` (`client/src/api.js`) choisissait le jeton d'après la simple PRÉSENCE de `admin_token` dans le navigateur ; un créateur (ex. `bestrong`) ouvrant `/generateur` dans le même navigateur que le panneau d'administration envoyait donc le jeton ADMIN et voyait — pire, pouvait modifier — les documents de l'admin. La portée est désormais **explicite** et posée par le composant qui rend le panneau : `setGeneratorScope(true)` via `variant="admin"` (`Admin.jsx`, onglet 📚), `variant="creator"` par défaut (`GeneratorPage.jsx`, page `/generateur`) avec remise à « creator » au démontage ; le serveur applique déjà la portée `owner_id` (404 hors propriétaire, l'admin voit tout) ; **changer de modèle de design semblait sans effet** : l'aperçu paginé restait en cache après un changement de modèle/styles/format (et le PDF exporté est calculé à partir de cet aperçu), d'où un PDF identique quel que soit le design choisi — l'aperçu est maintenant invalidé via `layoutKey` (modèle, `style_overrides`, format, dimensions, orientation, marges, table des matières) et reconstruit automatiquement ; cache PWA v294.

- **1.57.64** : **Générateur — marqueur « espace QR », détection des titres repensée, corrections publication** — **marqueur `[QR]`** : un paragraphe contenant exactement `[QR]` dans le contenu est remplacé par le QR code de vérification (boîte 40 mm insécable, traitée comme une image en pagination — jamais coupée entre deux pages) ; aperçu = cadre en pointillés (`.gen-qr-box`), PDF = QR réel (lien `/verifier/<réf>`) + cadre discret, EPUB = image QR dans le XHTML + manifeste ; bouton « ▣ Emplacement QR » dans la barre d'outils (`headings.js` → nouvelle logique de détection, testée par `tmp-structure-test.mjs`) ; **détection des titres repensée** (`structure.js`) : chapitres/sections/sous-titres détectés à l'import (numérotation, majuscules, listes), faux positifs évités ; **pagination livre** : seul le h1 (chapitre) ouvre une nouvelle page avec retrait en tête (CHAPTER_DROP), les sections restent dans le flux, aucun titre orphelin en bas de page ; **corrections suite aux retours de publication** : mention commission retirée du formulaire après publication, suppression TOTALE d'un fichier publié (bouton admin + créateur → `DELETE /api/generator/documents/:id/publish` : produit + objet Supabase bucket privé + lien `gen_documents.published_product_id` nettoyé), fiche produit publiée réparée (React #310 sur `ProductDetail` quand le produit venait du Générateur — `useEffect` conditionnel restructuré), image produit qui ne s'affichait pas, couverture PDF légèrement coupée en haut (cadrage vertical borné dans `coverImage.js`/`exportPdf.js`) ; cache PWA v295.


- **1.57.62** : **Générateur ouvert aux créateurs + conversion de devise à l'affichage** — **page `/generateur` pour les CRÉATEURS** (route `GeneratorPage.jsx` réutilisant le panneau, garde `CreatorSpace` : créateur avec adhésion active sinon `/adhesion`, ou admin ; lien « Générateur de documents » dans le menu) ; **serveur multi-utilisateur** (`roleRequired("admin","creator")`, portée stricte par `owner_id` — un créateur ne voit/modifie que SES documents, 404 sinon ; publication sous SON compte via `resolveOwnerId`, jeton utilisateur ou admin selon `generatorRequest` dans `api.js`) ; **publication produit digital enrichie** : prix + **commission vendeur %** (`commission_percent`, INSERT et UPDATE) + **catégorie digitale** (`DIGITAL_CATEGORIES` côté client : IA, Éducation, Développement personnel, Religion… miroir serveur `PUBLISH_CATEGORIES`, repli « Digital ») ; **images redimensionnables** dans l'éditeur (`GenImage.jsx`, extension TipTap + poignée de glissement 10-100 %, largeur persistée dans le HTML → pagination/aperçu/PDF/EPUB identiques) ; **options de couverture** : titre masquable, positions X/Y manuelles du titre (bug corrigé : le bloc pleine largeur était clampé au centre — `coverLayoutBox` rétrécit désormais le bloc à 62 % quand `titleX` est personnalisé), **opacité de l'image** (curseur, remplace le voile fixe `imageDim`) ; **modèles de design appliqués au PDF** (polices, décors, fonds, règles d'accent par modèle — validé par inspection du PDF binaire) ; **devise de publication selon le pays** (`defaultCurrencyFor(req.user.country)` côté serveur, symbole côté client — plus de XAF en dur) ; **réponses de l'assistant IA complètes** (`askAI` tronquait l'entrée à 2 000 car. et la sortie à 800 tokens : nouveaux `opts.maxInputChars`/`opts.maxOutputTokens`, Générateur 8 000/4 096, passage sélectionnable 4 000 car., chat/WhatsApp inchangés) ; **conversion de devise AFFICHAGE UNIQUEMENT** (`client/src/conversion.js` pur : XAF/XOF parité 1:1, EUR 655,957 fixe, USD/GNF/CDF indicatifs — sous le prix de ProductCard/ProductDetail/PurchasePage, « ≈ » dans la devise du visiteur connecté SEULEMENT si elle diffère de celle du produit, rien si taux inconnu ; aucun montant en base converti) ; cache PWA v293.





- **1.11.0 / v52** : masquage SEO complet, commission promo 0, partage promo, offres Verone dans l'accueil (rail), suppression commission duo.
- **1.12.0 / v53** : popup promos superposées avec rotation 5 s et titre « PROMOTION DU JOUR » ; correctif ancrage `bottom: 0` ; retrait des emojis 🎨/🎓 du menu.
- **1.13.0** : commission d'activation vendeur 1 000 XAF (parrainage vendeur) ; seuil de parrainage client porté à 5 000 XAF.
- **1.14.0** : suppression complète du système iKeePay (paiements en ligne, webhooks, reversements automatiques) ; passage au paiement manuel exclusif ; commissions et parrainages versés manuellement par la boutique ; facture PDF jsPDF.
- **1.49.1 / v194** : activation automatique des parrainés marqués « payés » + push commission au parrain via l'onglet Utilisateurs ; correction COALESCE (désapprobation ne marque plus l'adhésion payée).
- **1.50.0 / v195** : retour d'iKeePay en **PAYIN uniquement** (adhésion + don) avec **bascule admin manuel ↔ automatique** (`platform_settings`, panneau Admin → Système de paiement). En mode auto : checkout inline `ikeepay.com/checkout/v1/inline`, webhook `POST /api/ikeepay/webhook` (formats `payment.success` / `transaction.updated|created`), activation immédiate de l'adhésion + notification du parrain ; les versements (retraits d'activation, commissions) restent manuels. `server/fees.js` extrait les frais d'adhésion ; CSP Vercel (`vercel.json`) autorise l'origine iKeePay.
- **1.50.1 / v196** : correctif 500 sur l'enregistrement des clés iKeePay — le stockage des réglages devient **auto-réparant** (`setSetting` crée la table `platform_settings` à la volée si elle manque ; `donor_email` garanti dans `GET /api/admin/payments`).
- **1.50.2 / v197** : **accès direct à l'espace pour tous les rôles sauf le vendeur** (boutique, créateur, client, livreur — aucun frais, quel que soit le mode de paiement) ; panneau admin : sections **Utilisateurs** et **Retraits d'activation** toujours visibles dans les deux modes, seule la section **Parrainages** reste propre au mode manuel.
- **1.51.0 / v198** : **webhook iKeePay fiabilisé** — monté avant `originCheck` (un header Origin iKeePay ne le bloquait plus), lecture tolérante du corps brut (`req.rawBody`), **journalisation de chaque webhook reçu** dans `payment_webhook_logs` (visible dans le panneau Admin → Paiements en ligne → Journal des webhooks) ; **secours admin** pour marquer un don resté « en attente » comme complété (`POST /api/admin/payments/donations/:id/complete`).
- **1.51.1 / v199** : **webhook iKeePay réparé pour le format réel envoyé** — `payment.success` peut porter la référence imbriquée dans `data.external_reference` (et non seulement `order_id` racine) : `normalizeWebhook` cherche désormais la référence (et montant/devise) dans plusieurs champs, racine **et** `data`. Devise absente tolérée (la sécurité reste par référence + montant). Journal admin : bouton « Voir le payload » pour diagnostiquer le contenu exact reçu.
- **1.52.0 / v200** : **gestion des paiements en ligne côté admin** — bouton **« Supprimer »** sur chaque ligne (dons et adhésions) dans la section « Paiements en ligne (iKeePay) » ; **purge automatique** des paiements restés « en attente » depuis **plus de 30 minutes** (au chargement de la liste admin).
- **1.52.1 / v201** : **panneau admin (paiements en ligne)** — section « Journal des webhooks » retirée ; une adhésion d'un **vendeur parrainé** affiche le badge **« Vendeur parrainé »** et **son parrain** (nom + référence) ; recherche d'utilisateur par nom / email / **numéro de référence** (déjà couverte).
- **1.52.2 / v202** : **webhook adhésion fiabilisé** — parsing de la référence/montant/devise élargi (racine + `data` + `data.data`, plus de noms de champs) ; **réconciliation automatique par montant** : si la référence ne matche pas, on complète l'adhésion `pending` récente (≤ 30 min) du **même montant** → activation + redirection automatique ; **secours admin** pour compléter une adhésion manuellement (`POST /api/admin/payments/memberships/:id/complete`, bouton UI).
- **Post-1.52.2 (correctifs déployés sans bump de version)** :
  - **504 webhook adhésion corrigé** : push de notification parrain borné à 2,5 s (`Promise.race`) et boucle de réparation à budget temps (cooldown 5 s, max 10 logs, 4 s) — le webhook répond toujours vite, même en cas de lenteur push/DB ;
  - **webhook journalisé avant traitement** (plus d'angle mort si crash/timeout) ; diagnostic enrichi côté client (`[adhésion] statut : ...` + 5 derniers webhooks) ;
  - **purge → archivage** : les paiements en attente > 30 min passent en `expired` (invisibles côté admin mais réconciliables 24 h) au lieu d'être supprimés ;
  - **réconciliation 24 h par email prioritaire puis montant** (`pending` et `expired`) ; **filet d'activation** : un paiement `completed` < 24 h d'un compte non actif → activation à la volée ;
  - **polling client** : la page d'adhésion sonde `GET /api/payments/membership-status` toutes les 4 s (6 min) → redirection automatique vers l'espace dès activation ; **testé de bout en bout avec succès** (paiement → webhook → activation → redirection sans intervention admin) ;
  - **webhook protégé par token secret** (`ikeepay_webhook_secret` auto-généré ; `?k=` ou `x-ikeepay-token` ; 403 sinon) — **URL avec token à enregistrer chez iKeePay** (affichée/copiable dans le panneau Admin) ; correctif d'un import manquant (`getWebhookSecret`) qui provoquait un 500 sur `GET /api/admin/settings/payments` ;
  - **correctif « paiements en attente / redirection cassée »** : le déploiement v202 qui fonctionnait n'avait **pas** de token webhook ; après l'ajout du token (33dc0b8), toute URL non mise à jour chez iKeePay donne des **403 invisibles** (aucune trace, paiement jamais confirmé). Corrigé : webhooks 403 **journalisés** (`auth_rejected`, `handled=TRUE`) + **bloc « santé du webhook »** dans Admin → Paiements en ligne (reçus/rejetés 24 h, bandeau d'alerte) ; `getWebhookSecret` **ne régénère plus le secret** sur erreur de base de données (la bascule direct ↔ pooler Supabase pendant les déploiements v203→v208 pouvait invalider l'URL enregistrée chez iKeePay) et **503** quand le secret est indisponible (au lieu d'accepter un webhook à secret vide) ; réconciliation élargie à 30 logs. **Action admin si bandeau rouge : recopier l'URL de webhook (carte Système de paiement) et la mettre à jour dans le dashboard iKeePay.**
- Après 1.14.0 : correctifs méga-menu et catégories mobiles, retrait de PayPal de la page de soutien (remplacement par MoneyFusion), compteur d'adhésion 30 jours, libellés parrainage/admin, etc.

### Ajouts récents (post-1.57.46 — résumé)

- **1.57.52 / v283** : connaissances de l'assistante Vérone enrichies (produits digitaux, « qui publie quoi », page de soutien, renvoi vers le groupe WhatsApp de la communauté tant que le robot WhatsApp est indisponible).
- **1.57.53 / v284** : produits digitaux jusqu'à **50 Mo** par **upload direct navigateur → Supabase** (URL signée), quota par compte `DIGITAL_USER_QUOTA_MB` (**100 Mo en production**), carte admin « 💾 Stockage Supabase » (`GET /api/admin/storage/usage`) et purge des orphelins (`POST /api/admin/storage/purge-digital-orphans`).
- **1.57.54 / v285** : limite ramenée à **20 Mo par fichier** (`DIGITAL_MAX_BYTES`) et **2 produits digitaux par créateur** (`DIGITAL_MAX_PRODUCTS`, 403 `DIGITAL_PRODUCT_LIMIT`, duplication incluse) — **sauf comptes dispensés** (`DIGITAL_UNLIMITED_CREATORS`, « bestrong »).
- **Blocage global de l'adhésion activé** (`membership_gate = "all"`) : **vendeur 1 500 F, boutique et créateur 2 500 F / 30 jours** ; CGU (§ 2.1 « Adhésion et sécurité »), CGV (§ 3 : adhésion + produits digitaux), FAQ (« Qui doit payer l'adhésion ? », « Qui peut publier des produits ? », « Comment télécharger un produit digital ? »), README et connaissances de Vérone (fr/en/ar) alignés sur cette règle. Clients et livreurs restent gratuits.
