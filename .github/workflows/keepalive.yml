name: Keep-alive Vercel

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API health
        run: curl -fsS -o /dev/null --max-time 20 https://mboppi-mboppi.vercel.app/api/health