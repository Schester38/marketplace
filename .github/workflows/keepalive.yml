name: Keep-alive Vercel

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API health
        run: curl -fsS -o /dev/null --max-time 20 https://www.mboppishop.com/api/health